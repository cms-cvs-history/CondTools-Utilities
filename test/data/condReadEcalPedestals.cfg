  process condTEST = {
	path p = { get & print }

	es_source = PoolDBESSource { VPSet toGet = {
                                   {string record = "EcalPedestalsRcd"
                                     string tag = "ecalfall_test"
                                    } }
                                    string connect = "oracle://cms_val_lb.cern.ch/CMS_VAL_ECAL_POOL_OWNER"
			            string timetype = "runnumber" 
                                    untracked string catalog = "file:conddbcatalog.xml"
                                    untracked uint32 authenticationMethod = 1
				   }

	module print = AsciiOutputModule { }
	
	source = EmptySource {untracked int32 maxEvents = 10
                untracked uint32 firstRun = 1 
                untracked uint32 numberEventsInRun = 1}

	module get = EventSetupRecordDataGetter { VPSet toGet = {
	       {string record = "EcalPedestalsRcd"
	        vstring data = {"EcalPedestals"} } 
	       }
	       untracked bool verbose = true 
	}
}
